language: node_js
node_js:
  - lts/*

jobs:
  include:
    - stage: develop
      script:
        - npm run test:unit

    - stage: release
      if: branch = Release
      script:
        - npm run build
      
      # deploy:
      #   provider: heroku
      #   api_key:
      #   secure: "<HEROKU_GENERATED_ENCRYPTED_API_KEY>"
